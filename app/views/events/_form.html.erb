<div class="col s12 m12 l12 separadores"></div>
<div class="col s12 m12 l12 ">
  <div class="row">
    <div class="col s12 offset-m3 m6 offset-l4 l4">
      <div class="row">
        <%= form_for(@event) do |f| %>
          <% if @event.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>
              <ul>
              <% @event.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
              </ul>
            </div>
          <% end %>
<!--                 <div class="col s12">
            <input name="group1" type="radio" id="privado" />
            <label for="private">Private Event</label>                
            <input name="group1" type="radio" id="publico" />
            <label for="public">Public Event</label>
          </div> -->
          <div class="input-field col s12">
            <%= f.text_area :details, placeholder: 'Details', id: "details", class: "materialize-textarea" %>
            <label for="details">Details</label>
          </div>
          <div class="input-field col s12">
            <%= f.text_field :where, placeholder: 'Where', id: "where", class: "validate" %>
            <label for="where">Where</label>
          </div>

              <div class="col s6">
                <label class="" for="birthdate">Date</label>
                <%= f.date_field(:date, min: Date.today, class: "datepicker") %>
              </div>
              <div class="col s6">
                <!-- <label for="time">Time</label> -->
                <!-- <input type="time" name="hrs" placeholder="hrs:mins" value="" pattern="^([0-1]?[0-9]|2[0-4]):([0-5][0-9])(:[0-5][0-9])?$" class="inputs duration t1 time hrs" required> -->
              </div>


          <div class="file-field input-field col s12">
            <div class="btn">
              <span>Add Event Photo</span>
              <%= f.file_field :image, class: "file-path validate" %>
            </div>
          </div>

          <div class="col s12 center-align">
              <br>
              <%= f.submit "Create", class: "btn" %>
          </div>

        <% end %>
      </div>
    </div>
  </div>        
</div>
